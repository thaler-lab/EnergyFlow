language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
install:
  - pip install -U scipy
  - pip install cython
  - pip install pot
  - pip install pytest
  - pip install keras
  - pip install tensorflow
  - pip install .
script: 
  - pytest -v
branches:
  only:
    - master